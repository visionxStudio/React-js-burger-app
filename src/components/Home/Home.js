import React from 'react';
import '../Auth/Login/login.css'
const hero = () => {
    return (
        <section className="hero">
            <nav>
                <h2>Welcome</h2>
                <button>Logout</button>
            </nav>
        </section>
    )
}

export default hero;